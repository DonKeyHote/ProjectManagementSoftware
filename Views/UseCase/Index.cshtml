@model IEnumerable<ProjectManagementSoftware.Models.UseCase>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.UseCaseDesc)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CreatedDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CreatedByEmpNum)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ModifiedDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ModifiedByEmpNum)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UseCaseDesc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedByEmpNum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModifiedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModifiedByEmpNum)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UseCaseID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UseCaseID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UseCaseID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
